<div class="background">
	<div class = "container">
	</br>
		<ul>
	     <% @event.each do |u| %>
	       <li class="list-group-item list-group-item-danger list_event"><%= u.name %> - <%= u.description %> - <%= u.date.strftime("#{u.date.day.ordinalize} %B %Y") %> - <%= u.location %> - <%= u.starting_time.strftime(" Start: %H:%M")%> - <%= u.finishing_time.strftime(" Finish: %H:%M") %> - <%= link_to("Join this event", edit_event_path(id: :id))%>
	       </li>
	     <% end %>
	  </ul>
	 </div>
</div>
