<div>
	<ul>
     <% @user.each do |u| %>
       <li><%= u.name %> - <%= u.description %></li>
       <%= link_to("Edit user", edit_user_path(@user)) %> - 
		   <%= link_to("Delete user", user_index_path(@user), method: :delete, data: { confirm: 'Are you sure?'}) %>
     <% end %>
  </ul>
  <%= link_to("New user", new_user_path(@user)) %>
</div>